<div class="row">
	<%= render'layouts/sidebar' %>
	<div class="col-10 background px-0">
		<div class="text-center">
			<div class="col border-bottom border-dark" style="background-color:rgba(0,0,0,0.5);">
				<h1 class="top-text">Favorite diary</h1>
			</div>
		</div>
   <% if @diaries.count == 0 %>
      <div class="col text-center" style="background-color:white;">
        <h2>まだいいねをしてません</h2>
      </div>
   <% else %>
	    <%= render'layouts/timeline',diaries:@diaries%>
	 <% end %>
	</div>
</div>