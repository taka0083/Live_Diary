<div class="row justify-content-center" >
	<!-- user情報 -->
	<div class="col-6 my-3"style="border:solid 1px #000;">
		<div class="row ">
	      <div class="col-4 mt-1"><%= attachment_image_tag user, :user_image, format: 'jpeg'  ,class:"img-fluid rounded-circle" ,fallback: "no_image.jpg"%></div>
	      <div class="col-4 my-auto"><%= @user.name %></div>
	      	<!-- ログインが自分か他人か -->
	      <% if @user==current_user %>
	      	<div class="col-4 my-auto"><%= link_to "プロフィール編集", edit_user_path(user)%></div>
	      <% else %>

	      	<div class="col-4 my-auto">
            <% if current_user.following?(user) %>
              <%= link_to 'フォロー外す', unfollow_path(user.id), method: :POST %>
            <% else %>
              <%= link_to 'フォローする', follow_path(user.id), method: :POST %>
            <% end %>
	      	</div>
	      <% end %>
	  </div>
	  <div class="row">
	    <div class="col">フォロワー</div>
	    <div class="col">フォロー</div>
	    <div class="col">いいね</div>
	  </div>
	</div>
</div>